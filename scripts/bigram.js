

module.exports = {
  returnHistogram: (fileData) => {
    var histogram = {}
    fileData.forEach((word, i) => {
      if (fileData[i]) {
        // there is a word ahead of this one
      } else {

      }
    })
    return histogram
  }
}
